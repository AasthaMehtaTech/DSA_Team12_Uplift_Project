#include <iostream>
#include<math.h>
using namespace std;

int main()
{
int n,a[n];
cin>>p;

int x,y,sum=0,k=0;
for(int i=0;i<n;i++)
{
    
    while(p>0)
    {
     x=x%10;
     y=x*pow(2,k);
     a[i]=a[i]/10;
     k++;
     sum=sum+y;
    }
    cout<<sum<<" ";
}
}
